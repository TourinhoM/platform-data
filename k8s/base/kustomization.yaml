apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - service.yaml
  - statefulset.yaml

# Importante:
# - O Secret `postgresql-auth` e gerado/aplicado fora do Git (kubectl),
#   para nao versionar credenciais.
